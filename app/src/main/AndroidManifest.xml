<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ingreso" />
        <activity android:name=".Garantias" />
        <activity android:name=".Lista_Garantias" />
        <activity android:name=".Productos_Disponibles" />
        <activity android:name=".Ingreso_Producto" />
        <activity android:name=".Lista_Productos" />
        <activity android:name=".Clientes" />
        <activity android:name=".Ingresar_Cliente"></activity>
    </application>

</manifest>